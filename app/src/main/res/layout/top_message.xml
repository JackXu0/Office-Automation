<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="44dp"
    android:background="#000000"
    android:id="@+id/contact_main_bar"
    >

    <ImageView
        android:id="@+id/top_message_return_button"
        android:layout_width="23dp"
        android:layout_height="23dp"
        android:src="@drawable/return_button"
        android:layout_centerVertical="true"
        android:visibility="gone"
        android:layout_marginLeft="10dp"/>

    <ImageView
        android:id="@+id/expand"
        android:layout_width="21dp"
        android:layout_height="21dp"
        android:src="@drawable/homepage_expand"
        android:layout_centerVertical="true"
        android:layout_marginLeft="13dp"
        android:visibility="gone"/>

    <TextView
        android:id="@+id/top_message_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="通讯录"
        android:layout_centerInParent="true"
        android:textColor="#ffffff"
        />

    <Button
        android:id="@+id/contact_list_choose"
        android:layout_width="50dp"
        android:layout_height="25dp"
        android:layout_alignParentRight="true"
        android:layout_centerVertical="true"
        android:layout_marginRight="5dp"
        android:background="@drawable/btn_send_bg_disable"
        android:text="选择"
        android:visibility="invisible"
        android:textColor="#000000"
        android:textSize="16sp"
        />
    <Button
        android:id="@+id/personal_setting_expand_button"
        android:layout_width="25dp"
        android:layout_height="25dp"
        android:layout_alignParentRight="true"
        android:layout_centerVertical="true"
        android:layout_marginRight="15dp"
        android:visibility="gone"
        android:background="@drawable/personal_setting_expand_button"/>
</RelativeLayout>
